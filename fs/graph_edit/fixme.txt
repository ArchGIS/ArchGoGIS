- Согласовать опции в select элементе nodeTypeSelector.
  Сейчас там используются hardcoded значение "existing"/"new".

- Составить конвенции.

- Свойство startingNode нужно вынести из Node в graph.

- Переименовать свойство Node "key" в "isKey"
  Обновить устаревшие из-за этого комментарии.

